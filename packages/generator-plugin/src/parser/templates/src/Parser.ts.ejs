import { Parser } from '@openreceipt/core';

export default class <%= retailerNameCamelCase %><%= parserSince %> extends Parser {
  static readonly meta = {
    since: <%= parserSince %>,
    sourceAddress: '<%= parserEmail %>',
  };

  getId = () => {
  };

  getItems = () => {
  };

  getTaxes = () => {
  };

  getTotal = () => {
  };
}
